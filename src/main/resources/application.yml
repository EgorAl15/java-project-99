spring:
  application:
    name: app

  profiles:
    default: development

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET:development-secret-key-development-secret-key}